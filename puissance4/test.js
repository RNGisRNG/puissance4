function play_token(power4, classCol, classRow, colSelect) {
    const colTokens = classCol;
    const rowTokens = classRow;

    for (let j = 0; j < 7; j++) {
        column[j].addEventListener(("click"), () => {
            for(let i = 7; i > 0; i--){
                if(colTokens[5].used === 0 && rowTokens[6].used === 0){

                }
            }
        });
    }
}